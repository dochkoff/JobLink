@model JobServiceModel
@inject IEmployerService employerService
@inject IJobService jobService

<div class="col-md-4">
    <div class="card mb-3">
        <img class="card-img-top" src="@Model.CompanyLogoURL" alt="Company Logo" style="width: 100%; height: 200px; object-fit: cover;">
        <div class="card-body text-center">
            <h4>@Model.Title</h4>
            <h6>Location: <b>@Model.Location</b></h6>
            <h6>
                Monthly Salary:
                <b>@String.Format("{0:f2}", Model.Salary) BGN</b>
            </h6>
            <h6>(@(Model.IsApplied ? "Has Applications" : "Be The First"))</h6>
            <br />
            <a asp-controller="Job" asp-action="Details" asp-route-id="@Model.Id"
               asp-route-information="@Model.GetInformation()" class="btn btn-success">Details</a>
            @if (this.User.Identity.IsAuthenticated)
            {
                if (await jobService.HasEmployerWithIdAsync(Model.Id, User.Id()))
                {
                    <a asp-controller="Job" asp-action="Edit" asp-route-id="@Model.Id"
                       class="btn btn-warning">Edit</a>
                    <a asp-controller="Job" asp-action="Delete" asp-route-id="@Model.Id"
                       class="btn btn-danger">Delete</a>
                }

                <p></p>
                @if (!Model.IsApplied && (await employerService.ExistsByIdAsync(User.Id())) == false)
                {
                    <form class="input-group-sm" asp-controller="Job"
                          asp-action="Apply" asp-route-id="@Model.Id" method="post">
                        <input class="btn btn-primary" type="submit" value="Apply" />
                    </form>
                }
                else if (await jobService.IsAppliedByUserWithIdAsync(Model.Id, User.Id()))
                {
                    <form asp-controller="Job" asp-action="Leave"
                          asp-route-id="@Model.Id" method="post">
                        <input class="btn btn-primary" type="submit" value="Leave" />
                    </form>
                }
            }
        </div>
    </div>
</div>
